name: Daily Stock News Crawling

on:
  schedule:
    - cron: '23 21 * * *' # 매일 아침 06:23 (KST)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          # 🚀 크롤링을 위한 최소한의 도구만 설치합니다.
          pip install requests beautifulsoup4 lxml

      - name: Run Script
        env:
          EMAIL_ADDRESS: ${{ secrets.EMAIL_ADDRESS }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        run: python main.py
